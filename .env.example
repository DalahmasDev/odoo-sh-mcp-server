# Odoo.sh SSH Configuration
# Connect to your Odoo.sh instance via SSH

# SSH Host (format: user@project-id.odoo.sh or custom domain)
# Example: myuser@myproject.odoo.sh
ODOO_SH_SSH_HOST=your-project.odoo.sh

# SSH Port (default: 22)
ODOO_SH_SSH_PORT=22

# SSH Username (usually your Odoo.sh username)
ODOO_SH_SSH_USER=your_username

# Path to SSH private key
# IMPORTANT: Use absolute path to your SSH private key
# Example (Windows): C:\Users\YourName\.ssh\id_rsa
# Example (macOS/Linux): /home/yourname/.ssh/id_rsa
ODOO_SH_SSH_KEY_PATH=~/.ssh/id_rsa

# SSH Key Passphrase (optional, leave empty if no passphrase)
# RECOMMENDED: Use dynamic environment variable in Warp
# Example: $(security find-generic-password -a ssh-passphrase -w)
ODOO_SH_SSH_PASSPHRASE=

# SSH connection timeout in milliseconds
SSH_TIMEOUT=30000

# Logging level (debug, info, warn, error)
LOG_LEVEL=info
